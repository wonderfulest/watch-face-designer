// 应用全局配置
export default {
  // 自动保存相关配置
  autoSave: {
    // 自动保存间隔 (毫秒)
    interval: 10 * 1000, // 20秒
    // 是否启用自动保存
    enabled: false
  },
  
  // 可以添加其他全局配置
  api: {
    timeout: 30000
  },
  
  // 字体相关配置
  fonts: {
    // ...
  }
} 